/**
 * @format
 */

import {AppRegistry} from 'react-native';
import PrincipalRecetario from './src/screens/PrincipalRecetario';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => PrincipalRecetario);
